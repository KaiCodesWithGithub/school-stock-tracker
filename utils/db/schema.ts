/* eslint-disable @typescript-eslint/no-unused-vars */

import { items as itemsPg, bundles as bundlesPg, itemsToBundles as itemsToBundlesPg } from '../db-pgsql/schema'

export const items = itemsPg
export const bundles = bundlesPg
export const itemsToBundles = itemsToBundlesPg